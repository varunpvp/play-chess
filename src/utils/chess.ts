import Game from "../types/game";

export const makeMove = (game: Game, move: string): Game | null => {
  return game;
};

export const getColorToPlayFromFen = (fen: string): string => {
  return "";
};
